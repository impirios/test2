import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.mytestApplication',
  appName: 'Hot',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
